<% provide(:title, "User Dashboard") %>
<% provide(:heading, "Dashboard") %>
<%= current_user.inspect %>
<% slack_files.each do |file| %>
  <pre>
    <%= link_to( file["name"], file["url_download"]) + " | Type: #{file["pretty_type"]} | #{number_to_human_size(file["size"], precision: 2)}" %>
  </pre>
<% end %>